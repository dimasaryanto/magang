apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ing
  namespace: monitoring
spec:
  ingressClassName: nginx
  rules:
    - host: grafa.test.geschool.id
      http:
        paths:
          - backend:
              service:
                name: grafana-1638494931
                port:
                  number: 8080
            path: /
