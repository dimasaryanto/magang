apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-1633489984
  namespace: loki-grafana
spec:
  rules:
  - host: test.geschool.id
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: grafana-1633489984
            port:
              number: 80
