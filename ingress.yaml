apiVersion: networking.k8s.io/V1
kind: Ingress
metadata:
  name: loki-grafana
spec:
  rules:
   host: grafana.test
   http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: loki-grafana
            port:
              number: 80
